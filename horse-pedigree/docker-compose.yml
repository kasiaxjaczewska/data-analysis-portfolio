version: '3.8'
services:
  db:
    image: postgres:latest
    environment:
      POSTGRES_USER: horse_user
      POSTGRES_PASSWORD: Mandarynka242
      POSTGRES_DB: horse_pedigree
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data: